{
  "description": "opti-gsd tool usage analytics — logs all tool invocations for session analysis",
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "*",
        "hooks": [
          {
            "type": "command",
            "command": "node \"${CLAUDE_PLUGIN_ROOT}/scripts/log-tool-usage.js\" || true"
          }
        ]
      }
    ]
  }
}
